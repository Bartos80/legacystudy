<div class="container my-5">

  <!-- Encabezado + botones -->
  <div
    class="d-flex justify-content-between flex-column flex-md-row align-items-start align-items-md-center gap-3 mb-4">
    <h5 class="fw-bold text-primary text-uppercase m-0">📋 Listado de Juzgados</h5>
    <div class="d-flex flex-wrap gap-2 mt-2">
      <a href="/juzgados/add" class="btn btn-success btn-sm mr-4">+ Agregar Juzgado</a>
      <a href="/juzgados/listado" class="btn btn-outline-warning btn-sm">↻ Recargar</a>
    </div>
  </div>

  <!-- Formularios de búsqueda -->
  <div class="row g-3 mb-4">
    <div class="col-sm-6 col-md-3">
      <form action="/juzgados/findnombrecliente" method="POST">
        <input type="search" name="namecliente" class="form-control" placeholder="Buscar por nombre..." />
        <button class="btn btn-outline-secondary w-100 mt-2 btn-sm" type="submit">Buscar</button>
      </form>
    </div>
    <div class="col-sm-6 col-md-3">
      <form action="/juzgados/findapellidocliente" method="POST">
        <input type="search" name="lastnamecliente" class="form-control" placeholder="Buscar por apellido..." />
        <button class="btn btn-outline-secondary w-100 mt-2 btn-sm" type="submit">Buscar</button>
      </form>
    </div>
    <div class="col-sm-6 col-md-3">
      <form action="/juzgados/findlistadni" method="POST">
        <input type="search" name="dnicliente" class="form-control" placeholder="Buscar por DNI..." />
        <button class="btn btn-outline-secondary w-100 mt-2 btn-sm" type="submit">Buscar</button>
      </form>
    </div>
    <div class="col-sm-6 col-md-3">
      <form action="/juzgados/findlistafechaentrada" method="POST">
        <input type="search" name="date" class="form-control" placeholder="Buscar por fecha alta..." />
        <button class="btn btn-outline-secondary w-100 mt-2 btn-sm" type="submit">Buscar</button>
      </form>
    </div>
  </div>

  <!-- Tabla responsiva de juzgados -->
  {{#if juzgados.length}}
  <div class="table-responsive rounded shadow-sm border">
    <table class="table table-striped table-bordered align-middle text-nowrap">
      <thead class="table-primary text-dark text-center">
        <tr>
          <th>N° Juzgado</th>
          <th>Direccion</th>
          <th>Teléfono</th>
          <th>Email</th>  
        </tr>
      </thead>
      <tbody>
        {{#each juzgados}}
        <tr>
          {{!-- <td class="text-capitalize">{{ numjuzgado }}</td>
          <td>{{ dirjuzgado }}</td>
          <td>{{ teljuzgado }}</td>
          <td class="text-lowercase text-warning fw-bold">{{ emailjuzgado }}</td>   --}}
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
  {{else}}
  <div class="alert alert-warning text-center mt-5" role="alert">
    🚫 No existen Juzgados registrados.
  </div>
  {{/if}}
</div>