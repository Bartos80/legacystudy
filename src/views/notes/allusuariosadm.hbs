<script>
    function alertaborrar() {
        var mensaje;
        var opcion = confirm("Click en Borrar o Cancelar");
        if (opcion == true) {
            mensaje = "Elemento Borrado";
        } else {
            mensaje = "Cancelado";
        }
        document.getElementById("ejemplo").innerHTML = mensaje;
    }
</script>

<div class="container my-5">
  <div class="d-flex justify-content-between flex-column flex-md-row align-items-start align-items-md-center mb-4 gap-2">
    <h5 class="text-primary fw-bold text-uppercase m-0">
      👥 Sección de Usuarios - Administrador
    </h5>
    <div class="d-flex flex-wrap gap-2 mt-2">
      {{!-- <a href="/users/signup" class="btn btn-success btn-sm mr-4">+ Crear Usuario</a> --}}
      <a href="/usuarios" class="btn btn-outline-dark btn-sm">↻ Recargar Listado</a>
    </div>
  </div>

  {{#if usuarios.length}}
    <div class="table-responsive rounded shadow-sm border">
      <table class="table table-hover table-bordered align-middle text-nowrap">
        <thead class="table-primary text-dark text-center">
          <tr>
            <th scope="col">Nombre</th>
            <th scope="col">D.N.I.</th>
            {{!-- <th scope="col">Cód. Inspector</th> --}}
            <th scope="col">Función</th>
            <th scope="col">Rol</th>
            <th scope="col">Acciones</th>
          </tr>
        </thead>
        <tbody>
          {{#each usuarios}}
            <tr>
              <td class="text-capitalize">{{ name }}</td>
              <td>{{ dni }}</td>
              {{!-- <td>{{ codigoinspector }}</td> --}}
              <td class="text-capitalize">{{ funcion }}</td>
              <td>
                <span class="badge bg-secondary text-uppercase">{{ rolusuario }}</span>
              </td>
              <td>
                <div class="d-flex justify-content-center flex-wrap gap-1">
                  <a href="/usuario/list/{{_id}}" class="btn btn-outline-info btn-sm">+ Info</a>
                  <a href="/usuario/edit/{{_id}}" class="btn btn-outline-warning btn-sm">Editar</a>
                  <form action="/usuarios/delete/{{_id}}?_method=DELETE" method="POST"
                    onsubmit="return confirm('¿Desea borrar definitivamente al usuario?')">
                    <button class="btn btn-outline-danger btn-sm" type="submit">Borrar</button>
                  </form>
                </div>
              </td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  {{else}}
    <div class="alert alert-warning text-center mt-5" role="alert">
      🚫 No existen datos por mostrar.
    </div>
  {{/if}}
</div>
