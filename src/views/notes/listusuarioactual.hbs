<div class="container my-5">
  {{#each users}}
    <div class="card shadow-lg mx-auto" style="max-width: 600px;">
      <div class="card-header text-center bg-primary text-white">
        <h4 class="mb-0">👤 Usuario Actual Activo</h4>
      </div>

      <div class="card-body bg-light">
        <form>
          <div class="mb-3">
            <label class="form-label fw-bold text-secondary">ID:</label>
            <input type="text" class="form-control" value="{{_id}}" disabled>
          </div>

          <div class="mb-3">
            <label class="form-label fw-bold text-secondary">Nombres y Apellidos:</label>
            <input type="text" class="form-control" value="{{name}}" disabled>
          </div>

          <div class="mb-3">
            <label class="form-label fw-bold text-secondary">Código Usuario:</label>
            <input type="text" class="form-control" value="{{codigousuario}}" disabled>
          </div>

          <div class="mb-3">
            <label class="form-label fw-bold text-secondary">Función:</label>
            <input type="text" class="form-control" value="{{funcion}}" disabled>
          </div>

          <div class="mb-3">
            <label class="form-label fw-bold text-secondary">Correo Electrónico (E-Mail):</label>
            <input type="email" class="form-control" value="{{email}}" disabled>
          </div>

          <div class="mb-3">
            <label class="form-label fw-bold text-secondary">Rol del Usuario:</label>
            <input type="text" class="form-control" value="{{rolusuario}}" disabled>
          </div>

          <div class="mb-3">
            <label class="form-label fw-bold text-secondary">Fecha de Creación:</label>
            <input type="text" class="form-control" value="{{date}}" disabled>
          </div>

          <div class="text-center mt-4">
            <a href="javascript:window.history.back();" class="btn btn-outline-primary">
              &laquo; Volver Atrás
            </a>
          </div>
        </form>
      </div>
    </div>
  {{/each}}
</div>
