<script>
    function ponerminuscula() {
        let ex = document.getElementById("email");
        ex.value = ex.value.toLowerCase();
        //let y = document.getElementById("adrema");
        //y.value = y.value.toLowerCase();
    }

    function ShowName() {
        var writtenName = document.getElementById('name').value;
        document.getElementById('name').value = formatToString(writtenName);
    }

    function formatToString(writtenName) {
        var tmp = writtenName.split(" ");
        var fullName;
        tmp.forEach(element => {
            if (fullName) {
                fullName = fullName + " " + element.charAt(0).toUpperCase() + element.slice(1).toLocaleLowerCase();
            } else {
                fullName = element.charAt(0).toUpperCase() + element.slice(1).toLocaleLowerCase();
            }
        })
        return fullName;
    }

    function cargar() {
        var provincias = ["Cantabria", "Asturias", "Galicia", "Andalucia", "Extremadura"]; //Tu array de provincias
        var select = document.getElementById("provincias"); //Seleccionamos el select

        for (var i = 0; i < provincias.length; i++) {
            var option = document.createElement("option"); //Creamos la opcion
            option.innerHTML = provincias[i]; //Metemos el texto en la opción
            select.appendChild(option); //Metemos la opción en el select
        }
    }
    cargar();
</script>


<div class="col-md-5 mx-auto">
    <div class="card" style="font-style: italic;font-weight: bold;">
        <div class="card-header mx-auto">
            <p></p>
            <div class="card-body">
                <h4 style="color: rgb(26, 51, 189); font-weight: bold;">- Edición de Cliente -</h4>
                <form action="/notes/editcliente/{{clientes._id}}?_method=PUT" method="POST">
                    <input type="hidden" name="_method" value="PUT">
                    <div class="form-group">
                        Nombre y Apellido:
                        <input onkeyup="ShowName();" type="text" class="form-control" id="nyacliente"
                            name="nyacliente" value="{{clientes.nyacliente}}">
                    </div>
                    <div class="form-group">
                        Documento (D.N.I.):
                        <input type="number" class="form-control" id="dnicliente" name="dnicliente"
                            value="{{clientes.dnicliente}}">
                    </div>
                    <div class="form-group">
                        Domicilio:
                        <input type="text" class="form-control" id="domiciliocliente" name="domiciliocliente"
                            value="{{clientes.domiciliocliente}}">
                    </div>
                    <div class="form-group">
                        Provincia:
                        <input type="text" class="form-control" name="provincianocliente" id="provincianocliente"
                            value="{{clientes.provincianocliente}}">
                    </div>
                    <div class="form-group">
                        Localidad/Cuidad:
                        <input type="text" class="form-control" name="localidadcliente" id="localidadcliente"
                            value="{{clientes.localidadcliente}}">
                    </div>
                    <div class="form-group">
                        Telefono/Celular:
                        <input onkeyup="ponerminuscula()" id="telefonocliente" type="text" class="form-control"
                            name="telefonocliente" value="{{clientes.telefonocliente}}">
                    </div>
                    <div class="form-group">
                        Correo/Email:
                        <input onkeyup="ponerminuscula()" id="emailcliente" type="text" class="form-control"
                            name="emailcliente" value="{{clientes.emailcliente}}">
                    </div>

                    <div class="form-group">
                        Número de Carpeta (Opcional):
                        <input type="text" class="form-control" name="numerocarpetaopcionalcliente"
                            placeholder="Número de Carpeta" value="{{clientes.numerocarpetaopcionalcliente}}"/>
                    </div>

                    <div class="form-group">
                        Correo Vinculado al Estudio (Opcional):
                        <input type="text" class="form-control" name="correovinculadoclienteestudio"
                            placeholder="Correo Vinculado al Estudio" value="{{clientes.correovinculadoclienteestudio}}"/>
                    </div>                    

                    <div class="form-group">
                        Observaciones:
                        <input type="text" class="form-control" id="observacionescliente" name="observacionescliente"
                            placeholder="Observaciones" value="{{clientes.observacionescliente}}">
                    </div>
                    <div class="form-group">
                        <button class="btn btn-success btn-block" type="submit">
                            Grabar
                        </button>
                    </div>                    
                    {{!-- <br /> --}}
                    <a href="javascript:window.history.back();" style="color:blue; font-weight: bold;">
                        &laquo; Volver Atrás
                    </a>
                    <br /><br />
            </div>
            </form>
        </div>
    </div>
</div>