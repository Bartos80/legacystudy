<div class="container my-4">
  <!-- Filtros de búsqueda -->
  <div class="row g-3 mb-3">
    <div class="col-md-3">
      <form action="/Audiencia/findjuzgado" method="POST">
        <input type="search" class="form-control" name="juzgado" placeholder="Buscar x Juzgado..." />
        <button class="btn btn-outline-primary w-50 mt-2" type="submit">Buscar</button>
      </form>
    </div>

    <div class="col-md-3">
      <form action="/audiencia/findexpediente" method="POST">
        <input type="search" class="form-control" name="numexpediente"
          placeholder="Buscar x Expediente..." />
        <button class="btn btn-outline-primary w-50 mt-2" type="submit">Buscar</button>
      </form>
    </div>

    <div class="col-md-3">
      <form action="/audiencia/findcaratula" method="POST">
        <input type="search" class="form-control" name="caratula" placeholder="Buscar x Carátula..." />
        <button class="btn btn-outline-primary w-50 mt-2" type="submit">Buscar</button>
      </form>
    </div>

    <div class="col-md-3">
      <form action="/audiencia/findfecha" method="POST">
        <input type="search" class="form-control" name="dateturno" placeholder="Buscar por Fecha..." />
        <button class="btn btn-outline-primary w-50 mt-2" type="submit">Buscar</button>
      </form>
    </div>
  </div>

  <!-- Botones de acción --> 
  <div class="d-flex justify-content-between align-items-center mb-3"> 
    <a href="/Audiencia/listado/vencidas" class="btn btn-danger">+ Audiencias vencidas</a>
    <a href="/Audiencia/listado/proximas" class="btn btn-warning">+ Audiencias Próximas</a>
    <a href="/Audiencias/add" class="btn btn-success">+ Agregar Audiencia</a>
    <a href="/audiencia/listado" class="btn btn-outline-secondary btn-sm">↻ Recargar Listado Total</a>
  </div>

  <!-- Tabla de Audiencias -->
  {{!-- <div class="table-responsive">
    <table class="table table-striped table-hover align-middle border rounded shadow-sm">
      <thead class="table-light text-center"> --}}
        <!-- Tabla responsiva de clientes -->
        {{#if audiencias.length}}
        <div class="table-responsive rounded shadow-sm border">
          <table class="table table-striped table-bordered align-middle text-nowrap">
            <thead class="table-primary text-dark text-center">
              <tr>
                <th>Juzgado</th>
                <th>Expediente</th>
                <th>Estado</th>
                <th>Caratula</th>
                <th>Fecha</th>
                <th>Hora</th>
                <th>Opciones</th>
                <th>+Más</th>
              </tr>
            </thead>
            <tbody>
              {{#each audiencias}}
              <tr class="text-center">
                <td class="text-capitalize">{{ numjuzgado }}</td>
                <td>{{ numexpediente }}</td>
                <td>{{ estadoexpediente }}</td>
                <td>{{ caratula }}</td>
                <td class="text-lowercase text-danger fw-bold">{{ dateturno }}</td>
                <td class="text-lowercase text-danger fw-bold">{{ horaaudiencia }}</td>
                <td>
                  <div class="d-flex justify-content-center gap-2">
                    <a href="/audiencia/list/{{_id}}" class="btn btn-info btn-sm w-100">+Info</a>                    
                  </div>
                </td>
                <td>
                  <a href="/audiencia/edit/{{_id}}"  class="btn btn-outline-warning btn-sm w-100">Editar</a>
                  <form onSubmit="return confirm('Desea enviar Audiencia seleccionada a Papelera?')" style=""
                    action="/audiencia/marcadelete/{{_id}}?_method=PUT" method="POST">
                    <input type="hidden" name="_method" value="PUT">
                    <button class="btn btn-outline-danger btn-sm w-100"  type="submit">
                      Papelera
                    </button>
                  </form>
                </td>
              </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
        {{else}}
        <div class="alert alert-warning text-center mt-5" role="alert">
          🚫 No Hay Audiencias por mostrar - Pulse Recargar.
        </div>
        {{/if}}
        {{!-- </tbody>
    </table>
  </div> --}}
</div>