<div class="container my-5">

  <!-- Encabezado + botones -->
  <div
    class="d-flex justify-content-between flex-column flex-md-row align-items-start align-items-md-center gap-3 mb-4">
    <h5 class="fw-bold text-primary text-uppercase m-0">📋 Listado de estudios</h5>
    <div class="d-flex flex-wrap gap-2 mt-2">
      <a href="/estudios/add" class="btn btn-success btn-sm mr-4">+ Agregar</a>
      <a href="/estudios/listado" class="btn btn-outline-warning btn-sm">↻ Recargar</a>
    </div>
  </div>

  <!-- Formularios de búsqueda -->
  <div class="row g-3 mb-4">
    <div class="col-sm-6 col-md-3">
      <form action="//estudios/listados/findnumestudio" method="POST">
        <input type="search" name="numestudio" class="form-control" placeholder="Buscar por número de estudio..." />
        <button title="buscar Número de estudio" class="btn btn-outline-secondary w-100 mt-2 btn-sm" type="submit">Buscar</button>
      </form>
    </div>
    <div class="col-sm-6 col-md-3">
      <form action="//estudios/listados/findapellidocliente" method="POST">
        <input type="search" name="direstudio" class="form-control" placeholder="Buscar por dirección..." />
        <button title="buscar Dirección estudios" class="btn btn-outline-secondary w-100 mt-2 btn-sm" type="submit">Buscar</button>
      </form>
    </div>
    <div class="col-sm-6 col-md-3">
      <form action="//estudios/listados/findprovinciaestudio" method="POST">
        <input type="search" name="provinciaestudio" class="form-control" placeholder="Buscar por provincia..." />
        <button title="buscar Provincia" class="btn btn-outline-secondary w-100 mt-2 btn-sm" type="submit">Buscar</button>
      </form>
    </div>
    <div class="col-sm-6 col-md-3">
      <form action="//estudios/listados/findlocalidadestudio" method="POST">
        <input type="search" name="localidadestudio" class="form-control" placeholder="Buscar por localidad..." />
        <button title="buscar Localidad" class="btn btn-outline-secondary w-100 mt-2 btn-sm" type="submit">Buscar</button>
      </form>
    </div>
  </div>

  <!-- Tabla responsiva de /estudios/listados -->
  {{#if estudios.length}}
  <div class="table-responsive rounded shadow-sm border">
    <table class="table table-striped table-bordered align-middle text-nowrap">
      <thead class="table-primary text-dark text-center">
        <tr>
          <th>N° estudio</th>
          <th>Provincia</th>
          <th>Localidad</th>
          <th>Direccion</th>
          <th>Teléfono</th>
          <th>Email</th>  
          <td>Opciones</td>
          <td>+Más</td>
        </tr>
      </thead>
      <tbody>
        {{#each estudios}}
        <tr>
          <td class="text-capitalize">{{ numestudio }}</td>
          <td class="text-capitalize">{{ provinciaestudio }}</td>
          <td class="text-capitalize">{{ localidadestudio }}</td>
          <td>{{ direstudio }}</td>
          <td>{{ telestudio }}</td>
          <td class="text-lowercase text-danger fw-bold">{{ emailestudio }}</td>  
          <td>            
            <a class="btn btn-info btn-sm w-100" href="/estudios/list/{{_id}}"
              >+ Info</a>
          </td>
          <td>
              <a class="btn btn-warning btn-sm w-100"  href="/estudios/edit/{{_id}}">
                Editar
              </a>
              <form onSubmit="return confirm('Desea enviar Papelera?')" style=""
                action="/estudios/marcadelete/{{_id}}?_method=PUT" method="POST">
                <input type="hidden" name="_method" value="PUT">
                <button class="btn btn-danger btn-block btn-sm" type="submit">
                  Papelera
                </button>
              </form>
            </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
  {{else}}
  <div class="alert alert-warning text-center mt-5" role="alert">
    🚫 No existen estudios registrados.
  </div>
  {{/if}}
</div>