<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3>Caja Diaria: {{fechaFiltro}}</h3>
        <form action="/cobros/listado" method="GET" class="form-inline">
            <input type="date" name="fecha" value="{{fechaFiltro}}" class="form-control mr-2">
            <button type="submit" class="btn btn-primary">Filtrar</button>
        </form>
    </div>

    <div class="row mb-4">
        <div class="col-md-4">
            <div class="card bg-success text-white">
                <div class="card-body">
                    <h5>Total del Día</h5>
                    <h3>${{resumen.total}}</h3>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card bg-info text-white">
                <div class="card-body">
                    <h5>Efectivo</h5>
                    <h3>${{resumen.efectivo}}</h3>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card bg-secondary text-white">
                <div class="card-body">
                    <h5>Transferencias/Otros</h5>
                    <h3>${{resumen.transferencia}}</h3>
                </div>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Cliente</th>
                        <th>Concepto</th>
                        <th>Método</th>
                        <th>Monto</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each cobros}}
                    <tr>
                        <td>{{clienteId}}</td>
                        <td>{{concepto}}</td>
                        <td><span class="badge badge-light">{{metodoPago}}</span></td>
                        <td><strong>${{montoTotal}}</strong></td>
                        <td>
                            <a href="/cobros/comprobante/{{_id}}" class="btn btn-sm btn-outline-dark">
                                📄 Recibo
                            </a>
                        </td>
                    </tr>
                    {{else}}
                    <tr>
                        <td colspan="5" class="text-center">No hubo movimientos este día.</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</div>