<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-lg-10">
      <div class="card shadow-lg border-0 rounded-4">
        <div class="card-header bg-primary text-white text-center rounded-top-4 p-3">
          <h4 class="mb-0 fw-bold"><i class="fas fa-file-alt me-2"></i> Detalle y Vinculaciones del Expediente</h4>
        </div>

        <div class="card-body p-5">

          <h4 class="fw-bolder text-primary mb-4 pb-2 border-bottom d-flex align-items-center">
            <i class="fas fa-info-circle me-2"></i> Información General del Expediente
          </h4>

          <div class="row g-4 mb-5 p-4 rounded-4" style="background-color: #f7f9fc; border: 1px solid #e9ecef;">

            <div class="col-md-12">
              <h6 class="text-secondary border-bottom pb-1 mb-3">Identificación y Clasificación</h6>
              <div class="row g-3">

                {{!-- Los campos comentados se mantienen ocultos --}}
                {{!-- <div class="col-md-6 col-lg-3">
                  <label class="form-label text-muted mb-0 small">BD ID (MongoDB)</label>
                  <p class="mb-0 text-break text-secondary fw-semibold">{{expediente._id}}</p>
                </div>

                <div class="col-md-6 col-lg-3">
                  <label class="form-label text-muted mb-0 small">ID Expediente</label>
                  <p class="mb-0 text-break text-secondary fw-semibold">{{expediente.idexpediente}}</p>
                </div> --}}

                <div class="col-md-6 col-lg-3">
                  <label class="form-label fw-semibold mb-0">Nº Expediente</label>
                  <p class="mb-0 text-dark fw-bold fs-5">{{expediente.numexpediente}}</p>
                </div>

                <div class="col-md-6 col-lg-3">
                  <label class="form-label fw-semibold mb-0">Tipo</label>
                  <p class="mb-0 text-dark fs-5">{{expediente.tipo}}</p>
                </div>
              </div>
            </div>

            <div class="col-12 mt-4"> <label class="form-label fw-semibold mb-0">Carátula</label>
              <p class="mb-0 text-dark border p-2 rounded bg-white">{{expediente.caratula}}</p>
            </div>

            <div class="col-md-12 mt-4">
              <h6 class="text-secondary border-bottom pb-1 mb-3">Estado y Fechas</h6>
              <div class="row g-3">
                <div class="col-md-6 col-lg-4">
                  <label class="form-label fw-semibold mb-0">Última Notificación</label>
                  <p class="mb-0 text-muted">{{expediente.ultimanotificacion}}</p>
                </div>

                <div class="col-md-6 col-lg-4">
                  <label class="form-label fw-semibold mb-0">Estado</label>
                  <span class="badge bg-success fs-6">{{expediente.estado}}</span>
                </div>

                <div class="col-md-12 col-lg-4">
                  <label class="form-label fw-semibold mb-0">Partes (Resumen)</label>
                  <p class="mb-0 text-dark">{{expediente.partes}}</p>
                </div>
              </div>
            </div>

            <div class="col-md-12 mt-4">
              <h6 class="text-secondary border-bottom pb-1 mb-3">Roles y Demandantes</h6>
              <div class="row g-3">
                <div class="col-md-6">
                  <label class="form-label fw-semibold mb-0 text-success">Actor</label>
                  <p class="mb-0 text-dark fw-bold">{{expediente.actor}}</p>
                </div>

                <div class="col-md-6">
                  <label class="form-label fw-semibold mb-0 text-danger">Demandado</label>
                  <p class="mb-0 text-dark fw-bold">{{expediente.demandado}}</p>
                </div>
              </div>
            </div>

          </div>


          <h5 class="fw-bold text-info mt-4 mb-3 pb-2 border-bottom">🏛️ Juzgado Vinculado</h5>

          {{#if expedientejuzgado.length}}
          <div class="table-responsive rounded-3 shadow-sm border">
            <table class="table table-striped table-hover align-middle text-nowrap mb-0">
              <thead class="table-info text-dark text-center">
                <tr>
                  <th>N° Juzgado</th>
                  <th>Ubicación</th>
                  <th>País</th>
                  <th>Baja</th>
                  <th>Acciones</th>
                </tr>
              </thead>
              <tbody>
                {{#each expedientejuzgado}}
                <tr>
                  <td class="text-capitalize text-center fw-bold">{{ numjuzgado }}</td>
                  <td class="text-capitalize">{{ localidadjuzgado }}, {{ provinciajuzgado }}</td>
                  <td class="text-capitalize">{{ paisjuzgado }}</td>
                  <td class="text-center">{{ bajajuzgado }}</td>
                  {{!-- <td class="text-center">
                    <span class="badge bg-{{#if bajajuzgado}}danger{{else}}success{{/if}}">
                      {{#if bajajuzgado}}BAJA{{else}}ALTA{{/if}}
                    </span>
                  </td> --}}
                  <td class="text-center">
                    <div class="d-flex justify-content-center gap-1">
                      <form action="/juzgados/marcabajajuzgado/{{_id}}?_method=PUT" method="POST"
                        onsubmit="return confirm('¿Desea Dar de Baja?')">
                        <button class="btn btn-danger btn-sm" type="submit" title="Dar de Baja Juzgado" disabled><i class="fas fa-trash-alt"></i></button>
                        <input hidden id="idRetorno" name="idRetorno" value="{{expediente._id}}">
                      </form>
                      <form action="/juzgados/desmarcabajajuzgado/{{_id}}?_method=PUT" method="POST"
                        onsubmit="return confirm('¿Desea Recuperar?')">
                        <button class="btn btn-success btn-sm" type="submit" title="Dar de Alta Juzgado" disabled><i class="fas fa-check"></i></button>
                      </form>
                      <form action="/expedientes/marcadeleteexpedientesclientes/{{_id}}?_method=PUT" method="POST"
                        onsubmit="return confirm('¿Desea enviar a la papelera?')">
                        <button class="btn btn-outline-danger btn-sm" type="submit" title="Borrar Juzgado" disabled><i
                            class="fas fa-times"></i></button>
                      </form>
                    </div>
                  </td>
                </tr>
                {{/each}}
              </tbody>
            </table>
          </div>
          {{else}}
          <div class="alert alert-info text-center mt-3" role="alert">
            🏛️ Este expediente no tiene un Juzgado vinculado.
          </div>
          {{/if}}

          <hr class="my-5">

          <h5 class="fw-bold text-success mb-3 pb-2 border-bottom">👤 Clientes Vinculados</h5>

          {{#if expedientecliente.length}}
          <div class="table-responsive rounded-3 shadow-sm border">
            <table class="table table-striped table-hover align-middle text-nowrap mb-0">
              <thead class="table-success text-dark text-center">
                <tr>
                  <th>Nombre y Apellido</th>
                  <th>D.N.I.</th>
                  <th>Baja</th>
                  <th>Acciones</th>
                </tr>
              </thead>
              <tbody>
                {{#each expedientecliente}}
                <tr>
                  <td class="text-capitalize text-center">{{ nyacliente }}</td>
                  <td class="text-center">{{ dnicliente }}</td>
                  <td class="text-center">{{ bajacliente }}</td>
                  {{!-- <td class="text-center">
                    <span class="badge bg-{{#if bajacliente}}danger{{else}}success{{/if}}">
                      {{#if bajacliente}}BAJA{{else}}ALTA{{/if}}
                    </span>
                  </td> --}}
                  <td class="text-center">
                    <div class="d-flex justify-content-center gap-1">
                      <form action="/juzgados/marcabajajuzgado/{{_id}}?_method=PUT" method="POST"
                        onsubmit="return confirm('¿Desea Dar de Baja?')">
                        <button class="btn btn-danger btn-sm" type="submit" title="Dar de Baja Cliente" disabled><i
                            class="fas fa-trash-alt"></i></button>
                        <input hidden name="idRetorno" value="{{expediente._id}}">
                      </form>
                      <form action="/juzgados/desmarcabajajuzgado/{{_id}}?_method=PUT" method="POST"
                        onsubmit="return confirm('¿Desea Recuperar?')">
                        <button class="btn btn-success btn-sm" type="submit" title="Dar de Alta Cliente" disabled><i
                            class="fas fa-check"></i></button>
                      </form>
                      <form action="/expedientes/marcadeleteexpedientesclientes/{{_id}}?_method=PUT" method="POST"
                        onsubmit="return confirm('¿Desea enviar a la papelera?')">
                        <button class="btn btn-outline-danger btn-sm" type="submit" title="Eliminar" disabled><i
                            class="fas fa-times"></i></button>
                      </form>
                    </div>
                  </td>
                </tr>
                {{/each}}
              </tbody>
            </table>
          </div>
          {{else}}
          <div class="alert alert-info text-center mt-3" role="alert">
            👤 Este expediente no tiene Clientes vinculados.
          </div>
          {{/if}}

          <hr class="my-5">

          <h5 class="fw-bold text-warning mb-3 pb-2 border-bottom">⚖️ Abogados Vinculados</h5>

          {{#if expedienteabogado.length}}
          <div class="table-responsive rounded-3 shadow-sm border">
            <table class="table table-striped table-hover align-middle text-nowrap mb-0">
              <thead class="table-warning text-dark text-center">
                <tr>
                  <th>Nombre / Apellido</th>
                  <th>Matrícula</th>
                  <th>Baja</th>
                  <th>Acciones</th>
                </tr>
              </thead>
              <tbody>
                {{#each expedienteabogado}}
                <tr>
                  <td class="text-capitalize text-center">{{ nyaabogado }}</td>
                  <td class="text-center">{{ matriculaabogado }}</td>
                  <td class="text-center">{{ bajaabogado }}</td>
                  {{!-- <td class="text-center">
                    <span class="badge bg-{{#if bajaabogado}}danger{{else}}success{{/if}}">
                      {{#if bajaabogado}}BAJA{{else}}ALTA{{/if}}
                    </span>
                  </td> --}}
                  <td class="text-center">
                    <div class="d-flex justify-content-center gap-1">
                      <form action="/abogados/marcabajaabogados/{{_id}}?_method=PUT" method="POST"
                        onsubmit="return confirm('¿Desea Dar de Baja Abogado?')">
                        <button class="btn btn-danger btn-sm" type="submit" disabled title="Dar de Baja Abogado"><i
                            class="fas fa-trash-alt"></i></button>
                        <input hidden name="idRetorno" value="{{expediente._id}}">
                      </form>
                      <form action="/abogados/desmarcabajaabogado/{{_id}}?_method=PUT" method="POST"
                        onsubmit="return confirm('¿Desea Recuperar?')">
                        <button class="btn btn-success btn-sm" type="submit" disabled title="Dar de Alta Abogado"><i
                            class="fas fa-check"></i></button>
                      </form>
                      <form action="/expedientes/marcadeleteexpedientesabogados/{{_id}}?_method=PUT" method="POST"
                        onsubmit="return confirm('¿Desea enviar a la papelera?')">
                        <button class="btn btn-outline-danger btn-sm" type="submit" disabled title="Eliminar"><i
                            class="fas fa-times"></i></button>
                      </form>
                    </div>
                  </td>
                </tr>
                {{/each}}
              </tbody>
            </table>
          </div>
          {{else}}
          <div class="alert alert-info text-center mt-3" role="alert">
            ⚖️ Este expediente no tiene Abogados vinculados.
          </div>
          {{/if}}

        </div>
        <div class="card-footer bg-light text-center py-3 rounded-bottom-4">
          <a href="javascript:history.back();" class="btn btn-outline-secondary px-4 py-2">
            <i class="fas fa-arrow-left me-2"></i> Volver al Listado
          </a>
        </div>
      </div>
    </div>
  </div>
</div>