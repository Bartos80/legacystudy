<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="card shadow border-0">
        <div class="card-header bg-primary text-white text-center">
          <h4 class="mb-0 fw-semibold">Detalle del Expediente</h4>
        </div>
        <div class="card-body p-4">
          <div class="row g-3">
            <div class="col-md-6">
              <label class="form-label fw-semibold">BDIDExpediente</label>
              <p style="Color:grey" class="">{{expediente._id}}</p>
            </div>

            <div class="col-md-6">
              <label class="form-label fw-semibold">IDExpediente</label>
              <p style="Color:grey" class="">{{expediente.idexpediente}}</p>
            </div>

            <div class="col-md-6">
              <label class="form-label fw-semibold">Nº Expediente</label>
              <p style="Color:grey" >{{expediente.numexpediente}}</p>
            </div>

            <div class="col-md-6">
              <label class="form-label fw-semibold">Carátula</label>
              <p class="form-control-plaintext text-muted">{{expediente.caratula}}</p>
            </div>

            <div class="col-md-6">
              <label class="form-label fw-semibold">Tipo</label>
              <p class="form-control-plaintext text-muted">{{expediente.tipo}}</p>
            </div>

            <div class="col-md-6">
              <label class="form-label fw-semibold">Última Notificación</label>
              <p class="form-control-plaintext text-muted">{{expediente.ultimanotificacion}}</p>
            </div>

            <div class="col-md-6">
              <label class="form-label fw-semibold">Partes</label>
              <p class="form-control-plaintext text-muted">{{expediente.partes}}</p>
            </div>

            <div class="col-md-6">
              <label class="form-label fw-semibold">Estado</label>
              <p class="form-control-plaintext text-muted">{{expediente.estado}}</p>
            </div>         

            
          </div>
          <h5 class="fw-bold text-primary m-3">📋 Juzgado Vinculado al Expediente</h5>
          <!-- Tabla responsiva de /juzgados/listados -->
          {{#if expedientejuzgado.length}}
          <div class="table-responsive rounded shadow-sm border">
            <table class="table table-striped table-bordered align-middle text-nowrap">
              <thead class="table-primary text-dark text-center">
                <tr>
                  <th>N° Juzgado</th>
                  <th>Provincia</th>
                  <th>Localidad</th>
                  <th>De-Baja</th>
                  <th>Opciones</th>
                  <th>+</th>
                </tr>
              </thead>
              <tbody>
                {{#each expedientejuzgado}}
                <tr>
                  <td class="text-capitalize">{{ numjuzgado }}</td>
                  <td class="text-capitalize">{{ provinciajuzgado }}</td>
                  <td class="text-capitalize">{{ localidadjuzgado }}</td>
                  <td class="text-capitalize">{{ bajajuzgado }}</td>
                  <td>
                    <div class="d-flex flex-column gap-1">
                      <form action="/juzgados/marcabajajuzgado/{{_id}}?_method=PUT" method="POST"
                        onsubmit="return confirm('¿Desea Dar de Baja?')">
                        <button class="btn btn-danger btn-sm w-100" type="submit">Baja</button>
                        <input hidden id="idRetorno" name="idRetorno" value="{{expediente._id}}">
                      </form>
                      <form action="/juzgados/desmarcabajajuzgado/{{_id}}?_method=PUT" method="POST"
                        onsubmit="return confirm('¿Desea Recuperar?')">
                        <button class="btn btn-success btn-sm w-100" type="submit">Alta</button>
                      </form>
                    </div>
                  </td>
                  <td>
                  <div class="d-flex flex-column gap-1">
                    {{!-- <a href="/expedientes/edit/{{_id}}" class="btn btn-outline-warning btn-sm w-100">Editar</a> --}}
                    <form action="/expedientes/marcadeleteexpedientesclientes/{{_id}}?_method=PUT" method="POST"
                      onsubmit="return confirm('¿Desea enviar a la papelera?')">
                      <button class="btn btn-outline-danger btn-sm w-100" type="submit">Borrar</button>
                    </form>
                  </div>
                </td>
                </tr>

                {{/each}}
              </tbody>
            </table>
          </div>
          {{else}}
          <div class="alert alert-warning text-center mt-5" role="alert">
            🚫 No hay Juzgados por mostrar.
          </div>
          {{/if}}
        </div>
        <h5 class="fw-bold text-primary m-3">📋 Clientes Vinculados al Expediente</h5>
        <!-- Tabla responsiva de clientes -->
        {{#if expedientecliente.length}}
        <div class="table-responsive rounded shadow-sm border">
          <table class="table table-striped table-bordered align-middle text-nowrap">
            <thead class="table-primary text-dark text-center">
              <tr>
                <th style="text-align: center;">Nombre / Apellido</th>
                <th style="text-align: center;">D.N.I.</th>
                <th style="text-align: center;">Opciones</th>
                <th style="text-align: center;">+</th>
              </tr>
            </thead>
            <tbody>
              {{#each expedientecliente}}
              <tr>
                <td style="text-align: center;" class="text-capitalize">{{ nyacliente }}</td>
                <td style="text-align: center;">{{ dnicliente }}</td>
                <td style="text-align: center;">
                  <div class="d-flex flex-column gap-1">
                    <form action="/juzgados/marcabajajuzgado/{{_id}}?_method=PUT" method="POST"
                      onsubmit="return confirm('¿Desea Dar de Baja?')">
                      <button class="btn btn-danger btn-sm w-100" type="submit">Baja</button>
                      <input hidden id="idRetorno" name="idRetorno" value="{{expediente._id}}">
                    </form>
                    <form action="/juzgados/desmarcabajajuzgado/{{_id}}?_method=PUT" method="POST"
                      onsubmit="return confirm('¿Desea Recuperar?')">
                      <button class="btn btn-success btn-sm w-100" type="submit">Alta</button>
                    </form>
                  </div>
                </td>
                <td style="text-align: center;">
                  <div class="d-flex flex-column gap-1">
                    {{!-- <a href="/expedientes/edit/{{_id}}" class="btn btn-outline-warning btn-sm w-100">Editar</a> --}}
                    <form action="/expedientes/marcadeleteexpedientesclientes/{{_id}}?_method=PUT" method="POST"
                      onsubmit="return confirm('¿Desea enviar a la papelera?')">
                      <button class="btn btn-outline-danger btn-sm w-100" type="submit">Borrar</button>
                    </form>
                  </div>
                </td>
              </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
        {{else}}
        <div class="alert alert-warning text-center mt-5" role="alert">
          🚫 No Hay Clientes por mostrar.
        </div>
        {{/if}}
        <p></p>
        <h5 class="fw-bold text-primary m-3">📋 Abogados Vinculados al Expediente</h5>
        <!-- Tabla responsiva de abogados -->
        {{#if expedienteabogado.length}}
        <div class="table-responsive rounded shadow-sm border">
          <table class="table table-striped table-bordered align-middle text-nowrap">
            <thead class="table-primary text-dark text-center">
              <tr>
                <td>Nombre/Apellido</td>
                <td>Matricula</td>
                <td>Tel/Cel</td>
                <td>Email/Correo</td>
                <td>Localidad</td>
              </tr>
            </thead>
            <tbody>
              {{#each expedienteabogado}}
              <tr>
                {{!-- <td style="visibility:collapse;">{{ _id }}</td> --}}
                <td>{{ sector }}</td>
                <td style="text-transform: lowercase;">{{ nyaabogado }}</td>
                <td>{{ matriculaabogado }}</td>
                <td style="text-transform: lowercase;">{{ celularabogado }}</td>
                <td style="color:yellow;">{{ emailabogado }}</td>
                <td>{{ localidadabogado }}</td>
              </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
        {{else}}
        <div class="alert alert-warning text-center mt-5" role="alert">
          🚫 No hay Abogados por mostrar.
        </div>
        {{/if}}
        <div class="col-md-12">
          <label class="form-label fw-semibold">Foto del Expediente</label><br />
          {{#if expediente.fotoexpediente}}
          <img src="{{expediente.fotoexpediente}}" class="img-thumbnail mt-2" style="max-width: 200px;"
            alt="Foto del Expediente">
          {{else}}
          <p class="text-muted mt-2">Sin imagen disponible</p>
          {{/if}}
        </div>

        <div class="text-center mt-4">
          <a href="javascript:history.back();" class="btn btn-outline-danger">
            <i class="fas fa-arrow-left me-2"></i>Volver
          </a>
        </div>
      </div>
    </div>
  </div>
</div>